RewriteEngine On

# Keep existing files/directories directly accessible
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route all other requests to the PHP front controller
RewriteRule ^ index.php [QSA,L]
